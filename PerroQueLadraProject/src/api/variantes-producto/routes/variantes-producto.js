'use strict';

/**
 * variantes-producto router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::variantes-producto.variantes-producto');
